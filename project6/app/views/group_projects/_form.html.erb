
<%= form_with(model: group_project, local: true) do |form| %>
    <% if group_project.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(group_project.errors.count, "error") %> prohibited this project from being saved:</h2>
  
        <ul>
          <% group_project.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="field">
      <%= form.label :ProjectName %>
      <%= form.text_field :ProjectName %>
    </div>
  
    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>